import React from "react";

const Projects = () => {
  const sectionStyle = {
    height: "100vh",
    backgroundColor: "red",
  };
  return (
    <div style={sectionStyle} id="projects">
      Projects
    </div>
  );
};

export default Projects;
