import React from "react";

const Hero = () => {
  const sectionStyle = {
    height: "100vh",
    backgroundColor: "green",
  };
  return (
    <div style={sectionStyle} id="hero">
      Hero
    </div>
  );
};

export default Hero;
