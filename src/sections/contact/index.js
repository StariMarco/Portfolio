import React from "react";

const Contact = () => {
  const sectionStyle = {
    height: "100vh",
    backgroundColor: "blue",
  };
  return (
    <div style={sectionStyle} id="contact">
      Contact
    </div>
  );
};

export default Contact;
